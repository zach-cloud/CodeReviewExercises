This project is a simple method which determines if a number is between two other numbers.

The code works, but it contains a very common code-quality mistake made by beginner programmers. See if you can find it!